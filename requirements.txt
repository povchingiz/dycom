# Python 3.12 required (TotalSegmentator incompatible with 3.13/3.14)

# Core medical imaging
# dicom2nifti>=2.5 (pulled in by TotalSegmentator) imports pydicom.pixels,
# which only exists in pydicom 3.x — the old 2.4.4 pin broke every TotalSegmentator run.
pydicom>=3.0.0
SimpleITK==2.3.1
numpy>=1.26.4

# Segmentation
TotalSegmentator>=2.2.0

# Mesh processing
scikit-image>=0.22.0
trimesh>=4.3.2
# trimesh.proximity / registration.icp need an R-tree to do point-to-surface
# queries; without it Phase 5 validation raises ModuleNotFoundError at runtime.
rtree>=1.0.0
open3d>=0.18.0
scipy>=1.12.0
gmsh>=4.12.0
meshio>=5.3.0
fast-simplification>=0.1.13

# Dataset download (Phase 6 — HuggingFace subset fetch)
huggingface_hub>=0.23.0

# Data versioning
dvc>=3.0.0

# Web server (for demo)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
aiofiles>=23.2.1
python-dotenv>=1.0.0
