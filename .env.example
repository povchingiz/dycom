# FaceSim Demo Server Configuration
# Copy this file to .env and set your password

# Password for web demo access (change this!)
DEMO_PASSWORD=change_this_password

# Device for segmentation: cpu, cuda (NVIDIA GPU), or mps (Apple Silicon)
SEGMENTATION_DEVICE=cpu
